# Permissions & Restrictions

- Ne jamais modifier `.env` directement — utiliser `.env.example` comme référence
- Ne jamais commiter les clés API
- Les migrations Prisma doivent être validées avant d'être appliquées en prod
- Ne jamais supprimer de données utilisateur sans confirmation explicite
